<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    app:cardElevation="5dp"
    app:cardBackgroundColor="@android:color/transparent"> <!-- Hacemos el fondo del CardView transparente -->

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_top_card"> <!-- Fondo con esquinas redondeadas arriba -->

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/barrita"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:background="@drawable/bottom_sheet"/>

        <ScrollView
            android:id="@+id/scrollView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"> <!-- Asegurar que no tenga fondo -->

            <LinearLayout
                android:id="@+id/linearLayoutItems"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp"
                android:background="@android:color/transparent"/> <!-- Asegurar que no tenga fondo -->

        </ScrollView>

    </RelativeLayout>

</androidx.cardview.widget.CardView>
